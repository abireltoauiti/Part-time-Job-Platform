{# templates/job/_delete_form.html.twig #}
<form method="post" action="{{ path('job_delete', {'id': job.id}) }}" onsubmit="return confirm('Are you sure?');" style="display:inline-block;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
