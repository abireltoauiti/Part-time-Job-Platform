{# templates/job/_form.html.twig #}
{{ form_start(form) }}
    {{ form_row(form.titre) }}
    {{ form_row(form.description) }}
    {{ form_row(form.localisation) }}
    {{ form_row(form.salaire) }}
    {{ form_row(form.dateLimite) }}
<button class="btn btn-primary mt-2">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
